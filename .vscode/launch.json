{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Portfolio In Chrome",
            "request": "launch",
            "type": "pwa-chrome",
            "url": "http://localhost:8000",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "serve:python"
        }
    ]
}